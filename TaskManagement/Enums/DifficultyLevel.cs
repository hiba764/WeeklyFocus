namespace TaskManagement.Enums
{
    // التعداد (Enum) يحدد مدى صعوبة المهمة للمستخدم
    public enum DifficultyLevel : byte
    {
        Easy = 1,    // سهلة
        Medium = 2,  // متوسطة
        Hard = 3     // صعبة (تحتاج جهداً ووقتاً أكبر)
    }
}